namespace Miningcore.Blockchain.Ergo
{
    public class ErgoExtraNonceProvider : ExtraNonceProviderBase
    {
        public ErgoExtraNonceProvider(int size, byte? clusterInstanceId) : base(size, clusterInstanceId)
        {
        }
    }
}
